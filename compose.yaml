services:
  api:
    build: .
    image: taskman-api:v1
    container_name: taskman-api-c
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/database.db
    volumes:
      - ./data:/data
